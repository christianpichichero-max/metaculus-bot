# PINNED — CI reinstalls fresh every 20 minutes, and the bot overrides private SDK
# internals (_make_prediction, _aggregate_predictions), so a floating upstream release
# could silently drop geo-odds/supervisor behavior mid-tournament. Bump deliberately:
# test locally (pytest + --mode test_questions), then update the pin. Season rollover
# (CURRENT_*_ID constants) is an explicit, tested bump per CLAUDE.md.
forecasting-tools==0.2.92
openai==2.43.0
asknews==0.13.51
numpy==2.4.6
requests==2.34.2
python-dotenv==1.2.2
python-decouple==3.8
